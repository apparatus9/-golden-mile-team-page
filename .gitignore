# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Claude Code config (local tooling, not project code)
.claude/

# Node / build tooling (image optimization deps)
node_modules/

# Host CLI state (not project code)
.netlify/
.wrangler/

# Temporary conversion artifacts
IMG_*_small.*
*.HEIC
